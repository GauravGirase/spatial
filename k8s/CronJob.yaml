apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup
  namespace: production
spec:
  schedule: "0 2 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup
            image: backup-image
          restartPolicy: OnFailure
# aaj kal mera system
# sachin job se, time se con nai re?
